<?xml version="1.0" encoding="UTF-8"?>
<features name="drools">
	<feature name="drools-core" version="${pom.version}" resolver="(obr)" start-level='50'>
		<bundle>mvn:org.drools/drools-core-osgi/${pom.version}</bundle>
		<bundle>mvn:${brms.groupId}/mvel2/${brms.version}</bundle>
		<bundle>wrap:mvn:${brms.groupId}/ecj/${brms.version}</bundle>
	</feature>
	<feature name="drools-compilation" version="${pom.version}" resolver="(obr)" start-level='50'>
		<bundle dependency="true">mvn:org.drools/drools-core-osgi/${pom.version}</bundle>
		<bundle dependency="true">mvn:${brms.groupId}/mvel2/${brms.version}</bundle>
		<bundle dependency="true">wrap:mvn:${brms.groupId}/ecj/${brms.version}</bundle>
		<bundle>mvn:${brms.groupId}/drools-compiler/${brms.version}</bundle>
		<bundle>mvn:${brms.groupId}/drools-decisiontables/${brms.version}</bundle>
		<bundle>mvn:${brms.groupId}/drools-templates/${brms.version}</bundle>
		<bundle>wrap:mvn:${brms.groupId}/jxl/${brms.version}</bundle>
		<bundle>wrap:mvn:${brms.groupId}/antlr-runtime/${brms.version}</bundle>
		<bundle>mvn:commons-io/commons-io/2.4</bundle>
		<bundle>wrap:mvn:com.thoughtworks.xstream/xstream/1.4.4</bundle>
		<bundle>wrap:mvn:com.sun.xml.bind/jaxb-xjc/2.2.6</bundle>
	</feature>
	<feature name="drools-utils" version="${pom.version}" resolver="(obr)" start-level='50'>
		<bundle>mvn:${pom.groupId}/drools-utils/${pom.version}</bundle>
		<feature dependency="true">drools-core</feature>
		<feature dependency="true">drools-compilation</feature>
	</feature>
</features>